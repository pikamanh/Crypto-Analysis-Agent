services:
  api:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:8000:8000"
    command: uvicorn api.app:app --host 0.0.0.0 --port 8000
